 1.1.1-------2.2.5



\n代表换行



/*    */ 代表的是注释，其中内容不会被计算机计入



a=b和b=a 代表意思完全不同，是一个动作，代表右边赋给左边

 #include <stdio.h> 

 int main() 

 { 

 } 



<类型名称><变量名称>=<初始值>

eg: int price =0

      int amount =100



变量类型：int 表示可以存放怎么样的数据，且类型无法改变

需要运行的命令每行都要有分号



函数scanf:f表示formatted,格式化了的

\&price

在变量名称前要加\&符号

eg：scanf("%d",\&price);
其中除了%d占位符以外，其余部分在输入时要完全一致



直接写在程序里的数字叫做直接量（literal），最好定义一个常量const AMOUNT（一般常量大写以作提示）=100;  ，而非应用直接量，容易改且易理解

常量只能被定义一次



两个整数的运算结果只能是整数，若有小数部分，就全部舍弃（退一）

10.0是浮点数，和10完全不同

 Def:浮点数本意指小数点是浮动的，另一种叫做定点数（但c语言中没有），借用浮点数表示所有带小数点的数。 

 此时占位符要由%d改成%f 



double表示“双”，本用于“双精度浮点数”的第一个单词，用于表示浮点数类型

double在输入的时候用%lf,输出时用%f（不一样）

另有float也是浮点的意思，表示单精度浮点数。

前面int表示整数类型。





 数据类型： 

 整数：int    printf("%d",......)     scanf("%d",\&......) 

 浮点数：double   printf("%f",......)   scanf("%lf",\&......) 



运算符是指运算的动作，比如+-\*/

算子指参与运算的值，可能是常值，也可能是变量。



%代表取余运算，例如，10%3=1,      14%3=2



负数不需要打括号

赋值也是一种运算，并且优先级最低，和单目运算都是自右向左

复杂运算要通过拆解为简单写，不然不易阅读且容易出错



调试可以选中一行点击前面的数字，本行变红，再调试可以把当前变量数据显示出来，并且这一行（变蓝的）还未执行。

交换两个数据要引入第三个变量。



 作为后缀，其运算结果是一个副作用，运算结果不直接显现，但值已经改变；作为前缀，其运算结果直接显现。 



 if(    ){ 


 }  else{ 


 } 

 if,else条件的标准写法 

 也可以不用打括号，在（）后面加一句  





**3.2.1------------4.2.4 

关系运算，也作为一种运算符，其优先级比加减低，比赋值要高；==和！=的运算符比其他的低，而连续的关系运算是从左向右进行的。

==   相等

！=不相等

>大于

>=大于等于



两个值的关系符合预期，结果为1，否则为0



//后面的可以表示注释，和//效果差不多，前者表示单行，后者表示多行。


switch (控制表达式)  //只能是整数型的结果{
     case  常量/常值表达式：                     case 是一个分支标号，起到跳转的作用

     break                        遇到一个break程序才会离开switch语句，不然会执行到下一个case中去


     ......


     default
}


while 循环，基本格式和if差不多，区别就是if是一次性的，while是要反复的 ，且反复的过程包括判断条件
{   }内的句子叫做循环体
循环体内要有改变条件的机会，否则是死循环
要检验时可以在while中加一个printf，输出每一次循环中对应变量的值

do while 循环
先做一次再判断条件，若是满足，则和while循环相同；
 
 标准写法
 do
 {
   循环语句
 }while（循环条件）；


 for 循环
 for(初始条件A;循环继续的条件B;循环要做的动作C){
    （起点；终点；步进）
    D

 }
 像一个计数器，初始化他，在到达一定条件前，不断执行循环体，且内部的值改变一次，直到满足条件；
 for=对于，对于  A  ，当   B  时，重复   D   ，并在完成后使得    C

三个条件可以省掉一个留作空白,   但是;不能省

如果有固定的次数，用for
如果必须要执行一次，用do  while
其他的用while

break可以让我们跳出循环，
而只能跳出一次，可以加一个exit变量控制多次跳出循环（接力break）
而continue是跳过循环这一轮的剩下语句进入下一轮

可以循环嵌套


多重循环的时候
亦可以用goto语句，直接跳到某个位置 
goto后面接一个标号，
而出口的标号接一个冒号

要用到周期变化的数字时，可以用到一个变量，在循环中周期改变其的值

次方运算符在c中不能直接用
#include <math.h>
pow(a,b)     表示a^b



c语言的类型
整数
char  short  int  long  long long 

浮点数
float

逻辑
bool

指针

自定义类型

表达数的范围 char<short<int <float<double
内存所占的大小：一个字节到十六个字节

运算符：sizeof( ){括号内的不会参与运算，是一个静态运算符}
输出一个变量在计算机内所占的字节大小
一字节等于8比特

unsigned char 表示这个数没有补码的部分，这个数看为纯二进制
其初衷是为了做纯二进制的运算，主要为了移位

整数是以纯二进制进行运算的

%o代表八进制，%x代表十六进制，而其在计算机内部都是二进制来表示的

指定输出的小数的位数printf("%.2f",a);
其中.2表达要小数点后两位  

浮点数计算1/0时会输出无穷大，而0/0是nan表示不存在
而整数在做同样运算的时候会报错

带小数点的字面量是float而非double时要加f后缀表明身份
fabs（f1-f2）<1e-12

没有特殊需要用double

char可以是一种整数，也可以表达字符，用'  '表达字符的字面量
具体指的是ASCII码 
要输入输出时可以用%c
可以互相用ASCII码来转换

逃逸字符，两个字母连在一起表示一个意思
\b回退一格,若后面有字符，则后面的替换前面的，表现为删除一个，backspace；若后面没有字符，则无效果

\t表示制表位，指的是每一行的固定位置
一个\t使得输出从下一个制表位开始

\r表示回车

 #include <stdbool.h>
 可以用布尔运算
 bool是一种类型，用于判断，true用1表示，false用0来表示
 逻辑运算的结果只用0或1

！非
&&与（且）
||表示或

例如4<x<6用逻辑运算是  x<4&&x>6

逻辑运算自左向右，对于&&来说，左边是false则不做右边的；对于||来说，左边是true则不做右边。 称之为短路

不把赋值组合进表达式

条件运算符
count=(count>20)?count+=10:count-=10;
等价于
if(count>20){
  count+=10;
}else{
  count-=10;
}

前面括号中是判断条件，问号后是成立后要做的，冒号后是不成立后要做的
最好不用嵌套的条件表达式

逗号运算，用于连接两个表达式，以后面的式子作为结果；在for中会用到

函数是抽出一部分代码，输入0到多个数，并返回一个或是多个值
可以想象为数学中的y=f(x)
例
void sum(int first,int end)                   (函数头部) void是返回类型，sum是函数名称   （  ）代表这是一个函数   内部是参数表，有一个个参数
{
	int i;                                     
	int sum=0;                                  大括号内函数体，且括号不可省略
	for(i=first;i<=end;i++){
		sum+=i;                                   
	}
	printf("%d,%d,%d",first,end,sum);
}


void表示没有，这个函数不直接返回任何东西

要调用这个函数时，函数名称（参数值）;
例
sum（2,10）;

即使这个函数没有参数，也要有（），调用也同理
有参数时调用要给出正确数量和类型的数字，这些值会按顺序依次用来初始化函数中的参数

return 停止函数的执行，并送回一个值，作为一个出口，即为“函数值”

有返回值的时候用int，没有的时候用void

编译器从上而下分析代码，要用的函数要提前写

 函数的原型声明，可以把函数写在后面以凸显main函数，而把自定义函数的函数头粘贴在main前，且有分号
 先声明再定义
 声明中也可以没有函数的变量名称，只有变量类型

 局部变量优先
 有其生存期，在块————大括号内有效
 同一块内不能有相同的变量，不同块内可以
局部变量不会被默认初始化，而调用参数时一定要有初始化

要是确定函数无参数，则在括号中写void，最好不要留空白

调用函数时逗号是一个标点符号，而f（（ a，b））才会先做逗号运算

c语言不允许函数的嵌套定义

return时不要加括号，直接写函数值，否则容易误解为return函数

int number[100];
表达定义了一个数组，每一个数字的类型为int,且总共有100个数字
 <类型> 变量名称[元素数量]

[]里面的数字相当于每一个数字的代号，叫做下标或者索引，下标从0开始计数
一旦创建，不能改变其大小
内存中数组的元素是依次紧密排列的

数组符号可以在赋值号的左边或者右边，在右边时读取它的值[右值]，在左边时存入一个值[左值]

有效下标值：[0,数组大小-1]

长度为0的数组可以存在，但是无用

数组的集成初始化：
int a[]={2,3,5,8,5,2,5,2,5,12,}
可以不设定数组的大小，要是赋值的数字比设定大小要少，那么多余的数组位置上默认是0；
int a[10]={[0]=2,[4]=3,4}          此处也可不给数组大小
其中4赋给了[4]后面的[5]，而其他的没有赋值依然默认为0
适合初始化数据较为稀疏

sizeof(a)/sizeof(a[0])
表示数组中元素的个数
sizeof可以给出整个数组所占内容大小，单位是字节
这样即使修改初始数据，也不用去改遍历的代码

数组变量不能直接赋给另一个数组
int b[]=a;这是错的

要把一个数组赋给另一个数组的唯一方法是用循环遍历，一个一个赋值

遍历数组通常用for循环，让循环变量i从0到<数组的长度，刚好可以达到目的

二维数组
int a[3][5]
三行五列的矩阵
a[1][3]表示第一行第三列的元素

遍历需要两层循环
a[i,j]==a[j]

int a[][5]={
    {0,2,5,8,5},
    {5,8,4,8,8},
}；
其中列数（第二个数字）必须给出，行数可以由编译器去数
每一行一个{},用逗号分隔

&是一种运算符，取地址符
操作数要是变量，不能有运算
地址的大小是否和int相等取决于编译器

先写的地址更高，后写的低，地址自下而上分配
相邻的两变量地址差一个int，4个字节

指针就是保存地址的变量
通常用p(point)表示指针

int i;
int* p=&i;
p获得的是i的地址，即p指向了i
p里面的值就是i的地址

int *p,q;
表示p是一个指针，而q只是一个int 
即*不是加给int，而是p

指针变量的值是具有实际值的变量的地址
*是一个运算符，可以做右值，也可以做左值
*p作为一个整体，是一个整数
用*p=   可以跨函数来改变变量的值
跨函数先传递地址，再用地址来改变函数值


字符数组：

要使一个数组中的元素变成字符串，要在最后加一个\0，表达0
字符串是以0为结尾的一串字符；
结尾是0或者'\0'是一样的，但是和'0'不同
0不是字符串的一部分
计算长度的时候不包括这个0
以数组的形式存在，更多是以指针的形式
<string.h>

char *str="hello";
char word[]="hello";
char line[10]="hello";
编译器会自己生成一个0，位数要加一
“ ”内的东西叫做字符串的字面量或字符串的常量
两个相邻的字符串常量会被自动连接起来，成为一个大的字符串
中间可以加\表示上一个字符串未结束，达到限制程序宽度有作用

指针定义的字符串指向程序的代码段，并且是只读的，无法修改
初始化为指向一个字符串常量
char *s="hello,world";
实际上s==const chat *s
对s所指的字符做写入会有严重后果
 
char *s="hello,world";     作为指针指向这个字符串常量
char s[]="hello,world";    把字符串放在数组里
两种写法不同，一种只读，一种可以修改
第二种实际是把字符串常量拷贝到数组（本地变量）那里

数组表示这个字符串在这里，作为本地变量空间会被回收
指针表示这个字符串不知道在哪，用于动态分配空间，处理参数

构造字符串------>数组
处理字符串------>指针

字符串的复制，是使两个指针指向同一个地方
输入输出字符串的格式为%s，读入的是一个单词，到空格，tab，回车为止
要防止读入的字符大小比数组的大小大，可以在%s中间加数字%7s,
表示最多读进去7个字符
这个数字应该比数组的大小小

指针在定义的时候要初始化指向某个地方

char *a[]={
  "hello",
  "world",
}

表示一次性定义一个数组，数组中每一个元素都是一个指针，指针指向对应的字符串

putchar
int putchar(int c);
向标准输出写了一个字符
返回写了几个字符，EOF表示写失败

getchar
int getchar(void)

string.h

strlen()
内部需要const型的数组返回字符串的长度
例如
#include <stdio.h>
#include <string.h>
int main(int argc,char const *argv[])
{
	char a[]="hello";
	printf("%lu\n",strlen(a));
	printf("%lu\n",sizeof(a));	
	return 0;
}
结果是a的字符串长度为5，大小为6



strcmp
cmp代表compare，即比较两个字符串
int srtcmp(const char*s1,const char *s2);
返回值0，1，-1
0表示两个字符串相等
不相等的时候给出的就是两个的差值，本质是ASCII码


strcpy
char*strcpy(char*restrict dst,const char*restrict src);
作用是把后者的字符串拷贝到前者
restrict表明两个不能相等
目的放在前面，原字符串在后面
返回的就是dst


strchr
char*strchr(const char*s,int c);
char*sttrrchr(const char *s,int c);
返回NULL表示没有找到
前者是从前往后找，后者是从后往前找
返回的是一个指针，可以定义一个指针来表示，用%s输出指向的结果，即后面的所有字符

strstr
char*strstr(const char*s1,const char*s2);
在s1中寻找s2这个字符串
char*strcasestr(const char*s1,const*s2);
表示寻找过程中会忽略大小写

枚举是一种用户定义的数据类型
enum枚举类型名{名字0，……，名字n};
要用到的是大括号内的名字，每一个都是常量符号，类型为int，值从0到n

枚举的意义就是一次定义多个常量，有名字且可以定义初始值
例如	enum number{a,b,c,d=5,};
enum枚举类型名可以当作一种数据类型使用


预处理指令
#define
#include
由预处理器解释的，预处理器读入源代码，根据预处理指令进行修改，并将修改过的源代码递交给编译器
传地址调用称为引用，标量和常量按值传递
换行(newline) 故为/n
用双引号括起来的字符称为字符常量
NULL指一个其值为0的指针





